interface:
  display_name: "Rescue"
  short_description: "Legacy alias for delegating a task to Claude Code"
  default_prompt: "Use $claude-delegate for new delegation tasks; $claude-rescue remains available as a compatibility alias."
